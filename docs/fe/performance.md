# 前端性能

## 核心指标

* `LCP`(Largest Contentful Paint)： 最大内容绘制时间，测量用户可见的最大内容元素（如图片、视频、文本块）渲染完成的时间。用于衡量加载性能。
* `INP`(Interaction to Next Paint)： 交互到下一次绘制的时间，测量用户交互（如点击按钮）到下一次内容绘制的时间。用于衡量页面交互响应速度。
* `CLS`(Cumulative Layout Shift)： 累计布局偏移，测量页面布局改变导致的重新绘制次数。用于衡量视觉稳定性。

## 加载性能

## 交互性能

## 视觉稳定性